#include<iostream>

int main()
{
    std::cout<<"Hello World!";
    return 0;
}

//this text is just to test out sending to github.
//cout (said as C-OUT) = used to display something to the terminal, cout is inside of the iostream which you have to include/tell C++ to use as seen above.

/*
#include<string>
#include<iostream>
using std::cout;
using std::string;

void loop_string(string **str)

{
    string *s1 = *str;
    string s2 = *s1;
      string name = "mary";
      for (int i = 0; i < s2.length(); ++i)
      {
             s2[i] = name[i];
      }
} 

int main()
{

      string str = "john";
      string *strp = &str;
      string **strpp = &strp;

      loop_string(strpp);
      cout << str;
 
      return 0;
}
*/