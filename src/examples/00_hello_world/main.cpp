#include<iostream>

int main()
{
    std::cout<<"Hello World!";
    return 0;
}

//this text is just to test out sending to github.
//cout (said as C-OUT) = used to display something to the terminal, cout is inside of the iostream which you have to include/tell C++ to use as seen above.